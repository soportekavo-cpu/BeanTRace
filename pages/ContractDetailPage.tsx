import React from 'react';

// This component is no longer in use. Its functionality has been merged into
// ContractsPage.tsx to create a unified master-detail view.
// This file is kept to prevent import errors but can be safely removed
// if all references are updated.

const ContractDetailPage: React.FC = () => {
  return null;
};

export default ContractDetailPage;